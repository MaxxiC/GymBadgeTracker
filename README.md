# GymBadgeTracker

--TODO.

## Struttura del Progetto

Il progetto è organizzato nelle seguenti directory:

- **frontend:** Contiene l'applicazione React per il frontend.
- **backend:** Contiene il server Node.js per il backend.

## Prerequisiti

Assicurati di avere Node.js e npm installati sulla tua macchina.

## Installazione

1. Naviga nella directory `frontend` e esegui il comando:
   ```bash
   npm install

2. Naviga nella directory backend e esegui il comando:
   ```bash
   npm install

## Avvio del Progetto

1. Avvia il server Node.js per il backend:
   ```bash
   cd backend
   node index.js

2. Avvia l'app React per il frontend:
   ```bash
   cd frontend
   npm start